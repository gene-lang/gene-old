#!/usr/bin/env gene run

(class FibCalculator
  (method fib n
    (if (< n 2)
      n
    else
      (+ (self .fib (- n 1)) (self .fib (- n 2)))
    )
  )
)

(var calc (new FibCalculator))
(calc .fib 3)
