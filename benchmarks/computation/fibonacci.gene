# Fibonacci benchmark for Gene
(fn fib [n]
  (if (<= n 1)
    n
  else
    (+ (fib (- n 1)) (fib (- n 2)))))

(var n 24)
(println "Computing fib(" n ")...")
$duration_start
(var result (fib n))
(var elapsed_us $duration)
(println "fib(" n ") = " result)
(println "Time: " elapsed_us "us")
