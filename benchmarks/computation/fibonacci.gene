# Fibonacci benchmark for Gene
(fn fib [n]
  (if (<= n 1)
    n
  else
    (+ (fib (- n 1)) (fib (- n 2)))))

(var n 20)
(println "Computing fib(" n ")...")
# Note: time/now is not yet implemented, so timing is disabled
# (var start (time/now))
(var result (fib n))
# (var elapsed (- (time/now) start))
(println "fib(" n ") = " result)
# (println "Time: " elapsed "ms")
