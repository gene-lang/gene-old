---
phase: 00-unify-lifetime-and-publication-semantics
plan: 05
type: execute
wave: 4
depends_on:
  - "00-01"
  - "00-02"
  - "00-03"
  - "00-04"
files_modified:
  - src/gene/types/helpers.nim
  - src/gene/types/type_defs.nim
  - src/gene/vm/module.nim
  - tests/test_bootstrap_publication.nim
  - tests/integration/test_cli_gir.nim
  - tests/integration/test_thread.nim
  - tests/integration/test_stdlib_string.nim
  - tests/test_native_trampoline.nim
autonomous: true
requirements:
  - BOOT-01
must_haves:
  truths:
    - "Bootstrap-shared runtime artifacts have an explicit publication boundary after init"
    - "Runtime-created namespaces and classes remain outside the bootstrap-sharing guard"
    - "Phase 0 targeted regressions pass together after bootstrap discipline is added"
  artifacts:
    - path: "src/gene/types/helpers.nim"
      provides: "Bootstrap publication/freeze boundary in app/VM initialization"
    - path: "src/gene/vm/module.nim"
      provides: "Bootstrap-aware handling for any shared module publication path touched by the new invariant"
    - path: "tests/test_bootstrap_publication.nim"
      provides: "Focused bootstrap publication assertions"
  key_links:
    - from: "src/gene/types/helpers.nim"
      to: "tests/test_bootstrap_publication.nim"
      via: "Bootstrap publication guard is directly asserted by tests"
    - from: "src/gene/vm/module.nim"
      to: "src/gene/types/helpers.nim"
      via: "Module publication path either opts into the bootstrap boundary or remains explicitly actor-local"
---

<objective>
Enforce bootstrap publication discipline and run the phase acceptance sweep.

Purpose: Close Phase 0 with an explicit publication boundary for shared runtime
artifacts and verify the full substrate after the prior refactors land.
Output: Bootstrap publication guardrails, focused tests for the narrowed scope,
and a passing Phase 0 regression sweep.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-unify-lifetime-and-publication-semantics/00-CONTEXT.md
@src/gene/types/helpers.nim
@src/gene/types/type_defs.nim
@src/gene/vm/module.nim
@tests/integration/test_cli_gir.nim
@tests/integration/test_thread.nim
@tests/integration/test_stdlib_string.nim
@tests/test_native_trampoline.nim
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add focused bootstrap publication coverage</name>
  <files>tests/test_bootstrap_publication.nim, tests/integration/test_cli_gir.nim, tests/test_native_trampoline.nim</files>
  <read_first>src/gene/types/helpers.nim, src/gene/vm/module.nim, tests/integration/test_cli_gir.nim, tests/test_native_trampoline.nim</read_first>
  <action>Create `tests/test_bootstrap_publication.nim` for the narrowed Phase 0 invariant. Assert that bootstrap-shared artifacts become published only after init completes, and that runtime-created namespaces/classes are not incorrectly trapped behind that boundary. Reuse GIR/native tests when they already touch bytecode or native publication rather than duplicating coverage.</action>
  <verify>nim c -r tests/test_bootstrap_publication.nim</verify>
  <acceptance_criteria>
    - tests/test_bootstrap_publication.nim exists and covers the narrowed bootstrap scope
    - at least one assertion distinguishes bootstrap-published state from runtime-created namespaces or classes
  </acceptance_criteria>
  <done>The bootstrap publication invariant is directly testable instead of living only in the proposal text.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the narrowed bootstrap publication boundary</name>
  <files>src/gene/types/helpers.nim, src/gene/types/type_defs.nim, src/gene/vm/module.nim</files>
  <read_first>src/gene/types/helpers.nim, src/gene/types/type_defs.nim, src/gene/vm/module.nim, docs/proposals/actor-design.md</read_first>
  <action>Introduce the bootstrap publication/freeze boundary described in the proposal's narrowed invariant. Gate only the shared startup artifacts needed by later actor work: bytecode/code image publication, published JIT entry visibility, bootstrap registries, init-time namespace snapshots, and interned strings. If `src/gene/vm/module.nim` shares one of those publication slots, bring it under the same discipline; otherwise document in code why it remains actor-local for now.</action>
  <verify>rg -n \"bootstrap|frozen|publish|module cache|native_ready\" src/gene/types/helpers.nim src/gene/types/type_defs.nim src/gene/vm/module.nim && nim c -r tests/test_bootstrap_publication.nim</verify>
  <acceptance_criteria>
    - helpers.nim or its immediate collaborators define an explicit post-init publication boundary
    - runtime-created namespaces/classes are explicitly left out of the bootstrap-sharing guard
    - module.nim either opts into the guard or documents its actor-local status consistently with the proposal
  </acceptance_criteria>
  <done>The runtime has an explicit bootstrap publication boundary that matches the approved Phase 0 scope.</done>
</task>

<task type="auto">
  <name>Task 3: Run the Phase 0 acceptance sweep</name>
  <files>tests/integration/test_scope_lifetime.nim, tests/test_bootstrap_publication.nim, tests/integration/test_cli_gir.nim, tests/integration/test_thread.nim, tests/integration/test_stdlib_string.nim, tests/test_native_trampoline.nim</files>
  <read_first>tests/integration/test_scope_lifetime.nim, tests/test_bootstrap_publication.nim, tests/integration/test_cli_gir.nim, tests/integration/test_thread.nim, tests/integration/test_stdlib_string.nim, tests/test_native_trampoline.nim</read_first>
  <action>Run the focused Phase 0 suite end to end: lifetime, bootstrap publication, GIR/native publication, thread runtime, and string immutability. If any failure reveals a missing assumption between the prior plans, fix that gap before closing the phase.</action>
  <verify>nim c -r tests/integration/test_scope_lifetime.nim && nim c -r tests/test_bootstrap_publication.nim && nim c -r tests/integration/test_cli_gir.nim && nim c -r tests/integration/test_thread.nim && nim c -r tests/integration/test_stdlib_string.nim && nim c -r tests/test_native_trampoline.nim</verify>
  <acceptance_criteria>
    - the full focused Phase 0 regression suite passes
    - no remaining bootstrap/publication issue is deferred out of the plan
  </acceptance_criteria>
  <done>Phase 0 closes with its narrowed bootstrap invariant and full targeted regression sweep passing together.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] nim c -r tests/test_bootstrap_publication.nim
- [ ] nim c -r tests/integration/test_scope_lifetime.nim
- [ ] nim c -r tests/integration/test_cli_gir.nim
- [ ] nim c -r tests/integration/test_thread.nim
- [ ] nim c -r tests/integration/test_stdlib_string.nim
- [ ] nim c -r tests/test_native_trampoline.nim
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Bootstrap publication discipline matches the proposal's narrowed Phase 0 invariant
</success_criteria>

<output>
After completion, create `.planning/phases/00-unify-lifetime-and-publication-semantics/00-05-SUMMARY.md`
</output>
