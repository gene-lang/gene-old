---
phase: 04-remove-legacy-thread-first-concurrency-surfaces
plan: 01
type: execute
wave: 1
depends_on: [03]
files_modified:
  - src/gene/compiler/async.nim
  - src/gene/compiler/operators.nim
  - src/gene/vm/thread.nim
  - src/gene/vm/thread_native.nim
  - src/gene/stdlib/core.nim
  - tests/integration/test_thread.nim
autonomous: true
requirements: [ACT-04]
---

<objective>
Remove or explicitly retire the public thread-first language/runtime entrypoints while preserving the internal worker substrate that actors still use.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Lock legacy thread-surface removal behavior</name>
  <files>tests/integration/test_thread.nim</files>
  <action>Convert the legacy thread integration suite from “threads still work” into “thread-first public surfaces now fail or redirect with explicit migration behavior,” while keeping actor tests as the replacement confidence lane.</action>
  <verify><automated>nim c -r tests/integration/test_thread.nim</automated></verify>
</task>

<task type="auto">
  <name>Task 2: Remove public thread-first entrypoints</name>
  <files>src/gene/compiler/async.nim, src/gene/compiler/operators.nim, src/gene/vm/thread.nim, src/gene/vm/thread_native.nim, src/gene/stdlib/core.nim</files>
  <action>Remove or replace the public/compiler spellings for `spawn`, `spawn_return`, `Thread.send`, `Thread.send_expect_reply`, `Thread.on_message`, `send_expect_reply`, and `keep_alive` with explicit migration errors pointing to actors. Do not tear out the internal worker/channel substrate actors still require.</action>
  <verify><automated>nim c -r tests/integration/test_thread.nim && nim c -r tests/integration/test_actor_runtime.nim && nim c -r tests/integration/test_actor_reply_futures.nim</automated></verify>
</task>
</tasks>

<verification>
- `nim c -r tests/integration/test_thread.nim`
- `nim c -r tests/integration/test_actor_runtime.nim`
- `nim c -r tests/integration/test_actor_reply_futures.nim`
</verification>

<output>
After completion, create `.planning/phases/04-remove-legacy-thread-first-concurrency-surfaces/04-01-SUMMARY.md`
</output>
