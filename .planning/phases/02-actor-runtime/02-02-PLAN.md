---
phase: 02-actor-runtime
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/gene/vm/actor.nim
  - src/gene/types/helpers.nim
  - src/gene/vm/runtime_helpers.nim
  - src/gene/vm.nim
  - src/gene/stdlib/actor.nim
  - src/gene/stdlib/core.nim
  - tests/integration/test_actor_runtime.nim
autonomous: true
requirements:
  - ACT-02
must_haves:
  truths:
    - "The actor system can be enabled explicitly and rejects actor spawn before enable."
    - "Gene code spawns actors via `gene/actor/*`, not by repurposing bare `spawn`."
    - "A live actor processes one queued message at a time on the reused worker substrate."
  artifacts:
    - path: src/gene/vm/actor.nim
      provides: "Actor registry, mailbox/scheduler substrate, and worker dispatch entry points"
      contains: "proc"
    - path: src/gene/stdlib/actor.nim
      provides: "gene/actor namespace plus Actor and ActorContext classes"
      contains: "init_actor_namespace"
    - path: tests/integration/test_actor_runtime.nim
      provides: "Integration proof for enable/spawn/state-threading/single-message scheduling"
      contains: "suite"
  key_links:
    - from: src/gene/stdlib/actor.nim
      to: src/gene/vm/actor.nim
      via: "native spawn/enable entrypoints"
      pattern: "gene/actor"
    - from: src/gene/vm/actor.nim
      to: src/gene/vm/runtime_helpers.nim
      via: "worker bootstrap reuse"
      pattern: "create_thread_namespace|setup_thread_vm"
---

<objective>
Stand up the actor runtime substrate and the public `gene/actor/*` surface without breaking or rebranding the existing thread-first compatibility lane.

Purpose: give Phase 2 a real actor scheduler/API entrypoint on the existing worker/future substrate.
Output: actor runtime module, namespace bootstrap, enable/spawn behavior, and integration coverage for actor startup/scheduling.
</objective>

<execution_context>
@/Users/gcao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gcao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-actor-runtime/02-RESEARCH.md
@.planning/phases/02-actor-runtime/02-PATTERNS.md
@.planning/phases/02-actor-runtime/02-VALIDATION.md
@src/gene/vm/thread_native.nim
@src/gene/vm/runtime_helpers.nim
@src/gene/stdlib/core.nim
@src/gene/compiler/async.nim
@docs/handbook/freeze.md

<interfaces>
From `src/gene/vm/thread_native.nim`:
```nim
proc get_free_thread*(): int
proc init_thread*(thread_id: int, parent_id: int = 0)
proc cleanup_thread*(thread_id: int)
```

From `src/gene/vm/runtime_helpers.nim`:
```nim
proc create_thread_namespace*(thread_id: int): Namespace
proc setup_thread_vm(thread_id: int)
```

From `src/gene/compiler/async.nim`:
```nim
proc compile_spawn(self: Compiler, gene: ptr Gene)
  self.emit(Instruction(kind: IkSpawnThread))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the actor bootstrap contract in an integration harness</name>
  <files>tests/integration/test_actor_runtime.nim</files>
  <read_first>
    - .planning/phases/02-actor-runtime/02-VALIDATION.md
    - tests/integration/test_thread.nim
    - src/gene/compiler/async.nim
    - docs/proposals/future/actor_support.md
  </read_first>
  <behavior>
    - `gene/actor/spawn` fails while the actor system is disabled and succeeds after `gene/actor/enable`.
    - Actor handlers receive `(ctx msg state)` and return the next state.
    - Sequential sends observe one-message-at-a-time state progression.
    - Bare `spawn` keeps its thread semantics and is not rebound to the new actor API.
  </behavior>
  <action>Create `tests/integration/test_actor_runtime.nim` as the narrow execution harness for enable/spawn/state-threading/single-turn scheduling. Reuse the existing thread integration setup pattern and make the compatibility assertion explicit: Phase 2 exposes actors under `gene/actor/*` only.</action>
  <verify>
    <automated>nim c -r tests/integration/test_actor_runtime.nim</automated>
  </verify>
  <acceptance_criteria>
    - The integration file proves the locked API decision: actor entrypoints live under `gene/actor/*`.
    - The harness demonstrates actor enable-before-spawn and one-message-at-a-time scheduling expectations.
  </acceptance_criteria>
  <done>A dedicated integration gate exists for actor bootstrap and scheduling semantics before runtime code changes land.</done>
</task>

<task type="auto">
  <name>Task 2: Implement actor enable/spawn bootstrap and namespace wiring</name>
  <files>src/gene/vm/actor.nim, src/gene/types/helpers.nim, src/gene/vm/runtime_helpers.nim, src/gene/vm.nim, src/gene/stdlib/actor.nim, src/gene/stdlib/core.nim</files>
  <read_first>
    - .planning/phases/02-actor-runtime/02-RESEARCH.md
    - src/gene/vm/thread_native.nim
    - src/gene/vm/runtime_helpers.nim
    - src/gene/stdlib/core.nim
    - docs/handbook/freeze.md
  </read_first>
  <action>Create `src/gene/vm/actor.nim` to own actor runtime state, mailbox registration, ready/waiting/running scheduling, and worker pinning on top of the existing worker bootstrap. Wire that module into `src/gene/vm.nim`, initialize any VM-owned actor tables in `src/gene/types/helpers.nim`, and register a new `gene/actor` namespace plus `Actor` / `ActorContext` classes in `src/gene/stdlib/actor.nim` and stdlib bootstrap. Keep actors disabled by default until `gene/actor/enable` runs. Do not alias bare `spawn`, `spawn_return`, or thread methods; Phase 2 leaves those compatibility surfaces intact.</action>
  <verify>
    <automated>nim c -r tests/integration/test_actor_runtime.nim && nim c -r tests/integration/test_thread.nim</automated>
  </verify>
  <acceptance_criteria>
    - `gene/actor/enable` is a one-time bootstrap gate and actor spawn rejects use before enable.
    - Actor scheduling reuses worker bootstrap/helpers instead of creating a second concurrency runtime.
    - Thread syntax and `Thread` integration tests still compile and pass unchanged.
  </acceptance_criteria>
  <done>The repo has a real actor runtime/API entrypoint with explicit enable semantics and no Phase 2 regression to the legacy thread compatibility boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gene actor API → runtime scheduler | Untrusted Gene code can request actor creation and message scheduling here |
| actor runtime → worker bootstrap | Incorrect reuse of thread helpers can break isolation or worker liveness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Denial of Service | `src/gene/vm/actor.nim` | mitigate | Keep actors disabled until explicit `gene/actor/enable`, then reuse the existing bounded worker pool instead of creating unbounded workers |
| T-02-05 | Elevation of Privilege | `src/gene/stdlib/actor.nim` | mitigate | Expose actors only under `gene/actor/*`; do not silently retarget compiler bare `spawn` or thread methods |
| T-02-06 | Tampering | `src/gene/vm/runtime_helpers.nim` | mitigate | Reuse worker bootstrap helpers and per-thread namespace injection instead of mutating shared App state from actor startup paths |
</threat_model>

<verification>
Run the actor bootstrap integration plus thread regression:

- `nim c -r tests/integration/test_actor_runtime.nim`
- `nim c -r tests/integration/test_thread.nim`
</verification>

<success_criteria>
- Actors are explicitly enabled and spawned through `gene/actor/*`.
- One actor turn processes one message at a time on the reused worker substrate.
- Bare `spawn` remains a thread-only compatibility surface.
</success_criteria>

<output>
After completion, create `.planning/phases/02-actor-runtime/02-02-SUMMARY.md`
</output>
