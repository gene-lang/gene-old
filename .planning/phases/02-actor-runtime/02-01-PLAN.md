---
phase: 02-actor-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gene/types/type_defs.nim
  - src/gene/types/reference_types.nim
  - src/gene/types/core/native_helpers.nim
  - src/gene/types/runtime_types.nim
  - src/gene/vm/core_helpers.nim
  - tests/test_actor_runtime_types.nim
  - tests/test_extended_types.nim
autonomous: true
requirements:
  - ACT-02
must_haves:
  truths:
    - "Gene can represent Actor and ActorContext as first-class runtime values before scheduler work begins."
    - "The future actor API can resolve classes and type names without rebinding bare `spawn`."
  artifacts:
    - path: src/gene/types/type_defs.nim
      provides: "VkActor/VkActorContext kinds plus Application class slots"
      contains: "VkActor"
    - path: src/gene/types/reference_types.nim
      provides: "Reference payload storage for actor handle and actor context refs"
      contains: "of VkActor"
    - path: src/gene/vm/core_helpers.nim
      provides: "Class lookup for actor runtime values"
      contains: "of VkActor"
    - path: tests/test_actor_runtime_types.nim
      provides: "Focused regression coverage for actor type/class/runtime-name plumbing"
      contains: "suite"
  key_links:
    - from: src/gene/types/type_defs.nim
      to: src/gene/vm/core_helpers.nim
      via: "ValueKind-to-class dispatch"
      pattern: "of VkActor"
    - from: src/gene/types/core/native_helpers.nim
      to: src/gene/types/reference_types.nim
      via: "native converter boxing"
      pattern: "converter to_value"
---

<objective>
Define the runtime contracts that let later Phase 2 plans build on `Actor` and `ActorContext` as real Value kinds instead of ad hoc side tables.

Purpose: stabilize the type/runtime boundary before scheduler or transport code starts changing.
Output: actor kinds, boxed reference storage, converters, runtime type names, and class-resolution coverage.
</objective>

<execution_context>
@/Users/gcao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gcao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-actor-runtime/02-RESEARCH.md
@.planning/phases/02-actor-runtime/02-PATTERNS.md
@.planning/phases/02-actor-runtime/02-VALIDATION.md
@.planning/phases/01.5-freezable-closures/01.5-01-SUMMARY.md
@.planning/phases/01.5-freezable-closures/01.5-02-SUMMARY.md
@src/gene/types/type_defs.nim
@src/gene/types/reference_types.nim
@src/gene/types/core/native_helpers.nim
@src/gene/types/runtime_types.nim
@src/gene/vm/core_helpers.nim

<interfaces>
From `src/gene/types/type_defs.nim`:
```nim
  # Async types
  VkFuture
  VkGenerator
  VkThread
  VkThreadMessage
```

```nim
future_class*   : Value
generator_class*: Value
thread_class*   : Value
thread_message_class* : Value
thread_message_type_class* : Value
```

From `src/gene/vm/core_helpers.nim`:
```nim
of VkFuture:
  safe_class_value(App.app.future_class)
of VkThread:
  safe_class_value(THREAD_CLASS_VALUE)
of VkThreadMessage:
  safe_class_value(THREAD_MESSAGE_CLASS_VALUE)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock actor runtime type coverage before adding scheduler code</name>
  <files>tests/test_actor_runtime_types.nim, tests/test_extended_types.nim</files>
  <read_first>
    - .planning/phases/02-actor-runtime/02-PATTERNS.md
    - src/gene/types/type_defs.nim
    - src/gene/types/runtime_types.nim
    - tests/test_extended_types.nim
  </read_first>
  <behavior>
    - Test that `VkActor` and `VkActorContext` appear in the runtime kind surface and preserve enum/completeness expectations.
    - Test that actor values resolve stable runtime type strings and class lookup paths once boxed.
  </behavior>
  <action>Create focused regression coverage for the new actor kinds before implementation. Extend `tests/test_extended_types.nim` only for enum/order invariants, and keep actor-specific behavior in `tests/test_actor_runtime_types.nim` so later plans can use it as the narrow compile gate for the new runtime surface.</action>
  <verify>
    <automated>nim c -r tests/test_actor_runtime_types.nim && nim c -r tests/test_extended_types.nim</automated>
  </verify>
  <acceptance_criteria>
    - Actor/runtime type tests fail before the new ValueKind and class plumbing exists.
    - The baseline enum completeness check explicitly mentions the new actor kinds.
  </acceptance_criteria>
  <done>Focused type-plumbing coverage exists and is runnable independently of later scheduler/send work.</done>
</task>

<task type="auto">
  <name>Task 2: Add first-class Actor and ActorContext runtime plumbing</name>
  <files>src/gene/types/type_defs.nim, src/gene/types/reference_types.nim, src/gene/types/core/native_helpers.nim, src/gene/types/runtime_types.nim, src/gene/vm/core_helpers.nim</files>
  <read_first>
    - .planning/phases/02-actor-runtime/02-RESEARCH.md
    - src/gene/types/type_defs.nim
    - src/gene/types/reference_types.nim
    - src/gene/types/core/native_helpers.nim
    - src/gene/vm/core_helpers.nim
  </read_first>
  <action>Add `VkActor` and `VkActorContext` beside the existing async/thread kinds, add Application class slots for the two new classes, store actor and actor-context payloads in `Reference`, add `to_value` converters, extend runtime type-name reporting, and extend value-to-class resolution. Do not alias or alter the existing thread kinds here; this plan only creates the contracts that later scheduler/API work will consume.</action>
  <verify>
    <automated>nim c -r tests/test_actor_runtime_types.nim && nim c -r tests/test_extended_types.nim</automated>
  </verify>
  <acceptance_criteria>
    - `Actor` and `ActorContext` can be boxed/unboxed through the same converter/reference path used by `Thread` and `Future`.
    - Runtime type names and class resolution cover the new kinds without changing existing thread behavior.
  </acceptance_criteria>
  <done>The runtime type system can represent actor handles and actor contexts cleanly enough for later plans to build scheduler, API, and Future wiring on top.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stdlib/native API → runtime type system | Untrusted Gene code reaches the new actor handle/context types here |
| boxed Value → class resolution | Incorrect kind/class mapping would mis-route methods or expose wrong runtime behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `src/gene/types/type_defs.nim` | mitigate | Define dedicated `VkActor`/`VkActorContext` kinds instead of reusing thread kinds or opaque `VkCustom` payloads |
| T-02-02 | Spoofing | `src/gene/vm/core_helpers.nim` | mitigate | Resolve actor values through explicit class slots, mirroring the existing `Thread` class-dispatch pattern |
| T-02-03 | Denial of Service | `tests/test_actor_runtime_types.nim` | accept | Compile-time type tests are low-risk and do not expose runtime attack surface beyond normal CI cost |
</threat_model>

<verification>
Run the focused actor-type compile gate:

- `nim c -r tests/test_actor_runtime_types.nim`
- `nim c -r tests/test_extended_types.nim`
</verification>

<success_criteria>
- `Actor` and `ActorContext` are represented as first-class runtime kinds.
- The runtime can box, name, and class-resolve actor values without touching the legacy thread API surface.
- The focused actor-type tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-actor-runtime/02-01-SUMMARY.md`
</output>
