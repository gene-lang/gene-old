---
phase: 02-actor-runtime
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/gene/vm/actor.nim
  - src/gene/stdlib/actor.nim
  - tests/test_phase2_actor_send_tiers.nim
autonomous: true
requirements:
  - ACT-02
must_haves:
  truths:
    - "Send behavior distinguishes primitive, frozen, frozen-closure, and mutable payloads."
    - "Frozen payloads reuse the Phase 1 / 1.5 `deep_frozen/shared` contract instead of reintroducing serializer gates."
    - "Mailbox pressure never blocks an actor worker thread; parked actor senders resume when capacity frees."
  artifacts:
    - path: src/gene/vm/actor.nim
      provides: "Tiered send path, mailbox capacity handling, and mutable clone logic"
      contains: "deep_frozen"
    - path: tests/test_phase2_actor_send_tiers.nim
      provides: "Unit/stress coverage for actor payload routing and mailbox backpressure"
      contains: "suite"
  key_links:
    - from: src/gene/vm/actor.nim
      to: docs/handbook/freeze.md
      via: "deep_frozen/shared gate"
      pattern: "deep_frozen|shared"
    - from: src/gene/vm/actor.nim
      to: .planning/phases/01.5-freezable-closures/01.5-02-SUMMARY.md
      via: "frozen closure send-by-pointer contract"
      pattern: "VkFunction"
---

<objective>
Implement the Phase 2 transport rules so actor send semantics are total for ordinary data, cheap for frozen data, and safe for mutable graphs under bounded mailboxes.

Purpose: land the core actor-send contract before reply and stop behavior extend it.
Output: tiered send implementation, mailbox backpressure policy, and focused send-tier regression coverage.
</objective>

<execution_context>
@/Users/gcao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gcao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-actor-runtime/02-RESEARCH.md
@.planning/phases/02-actor-runtime/02-PATTERNS.md
@.planning/phases/02-actor-runtime/02-VALIDATION.md
@.planning/phases/01.5-freezable-closures/01.5-02-SUMMARY.md
@docs/handbook/freeze.md
@src/gene/types/core/value_ops.nim
@src/gene/types/memory.nim
@src/gene/stdlib/freeze.nim

<interfaces>
From `docs/handbook/freeze.md`:
```text
Frozen values are the values the actor design can share by pointer.
Phase 2 builds on this closure-freeze rule instead of rediscovering it in send logic.
```

From `src/gene/types/core/value_ops.nim` / `src/gene/types/memory.nim`:
```nim
proc deep_frozen*(v: Value): bool
proc shared*(v: Value): bool
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add send-tier and mailbox-pressure regression coverage</name>
  <files>tests/test_phase2_actor_send_tiers.nim</files>
  <read_first>
    - .planning/phases/02-actor-runtime/02-VALIDATION.md
    - tests/test_phase15_freezable_closures.nim
    - tests/test_phase15_shared_closure_heap.nim
    - docs/handbook/freeze.md
  </read_first>
  <behavior>
    - Primitive payloads transfer by value.
    - Deep-frozen graphs and frozen closures transfer by pointer-safe shared semantics.
    - Mutable graphs deep-clone while preserving alias structure and reusing frozen subgraphs.
    - Capability values fail clearly.
    - Full mailboxes park actor senders instead of blocking worker threads; non-actor entrypoints may block until space frees.
  </behavior>
  <action>Create `tests/test_phase2_actor_send_tiers.nim` as the focused Phase 2 transport gate. Use unit/stress tests to pin the mailbox-full policy and the locked mutable-clone rule: mutable send clones live on the shared heap with `shared=false`, while frozen subgraphs are pointer-shared.</action>
  <verify>
    <automated>nim c -r --threads:on tests/test_phase2_actor_send_tiers.nim</automated>
  </verify>
  <acceptance_criteria>
    - The tests distinguish primitives, frozen graphs, frozen closures, mutable graphs, and capability rejection.
    - Mailbox-pressure assertions explicitly encode actor-sender parking instead of worker-thread blocking.
  </acceptance_criteria>
  <done>A dedicated Phase 2 send-tier suite exists and fails until the tiered transport rules are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement tiered send routing and bounded-mailbox backpressure</name>
  <files>src/gene/vm/actor.nim, src/gene/stdlib/actor.nim</files>
  <read_first>
    - .planning/phases/02-actor-runtime/02-RESEARCH.md
    - .planning/phases/01.5-freezable-closures/01.5-02-SUMMARY.md
    - docs/handbook/freeze.md
    - src/gene/types/core/value_ops.nim
    - src/gene/types/memory.nim
  </read_first>
  <action>Extend the actor runtime to route payloads by tier: primitives by value, `deep_frozen/shared` graphs by pointer with shared-RC semantics, and mutable graphs by deep clone while pointer-sharing frozen subgraphs. Consume Phase 1.5 frozen closures strictly through the existing `deep_frozen/shared` check; do not reintroduce serializer-based closure transport. Implement bounded mailboxes with parked actor senders and allow blocking only from non-actor entrypoints, per the locked Phase 2 defaults.</action>
  <verify>
    <automated>nim c -r --threads:on tests/test_phase2_actor_send_tiers.nim && nim c -r tests/integration/test_actor_runtime.nim</automated>
  </verify>
  <acceptance_criteria>
    - The runtime never shares mutable sender-owned storage across actors.
    - Frozen closures use the same fast path as other frozen graphs.
    - Backpressure behavior matches the locked mailbox policy and keeps actor workers non-blocking.
  </acceptance_criteria>
  <done>Actor send semantics now follow the approved primitive/frozen/mutable split with bounded-mailbox behavior that is safe for worker reuse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sender actor → receiver actor | Cross-actor payload transfer crosses from one isolation domain to another here |
| runtime clone/share path → mailbox queue | Bad routing here can leak mutable aliases or stall workers under pressure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | `src/gene/vm/actor.nim` send path | mitigate | Gate pointer-sharing on `deep_frozen/shared` only and deep-clone mutable graphs with alias tracking |
| T-02-08 | Elevation of Privilege | actor capability transport | mitigate | Reject capability/native resource values with explicit runtime errors instead of serializing or pointer-sharing them |
| T-02-09 | Denial of Service | bounded mailbox handling | mitigate | Park actor senders on full mailboxes and allow blocking only from non-actor entrypoints so worker threads are not pinned |
</threat_model>

<verification>
Run the focused send-tier suite and actor runtime regression:

- `nim c -r --threads:on tests/test_phase2_actor_send_tiers.nim`
- `nim c -r tests/integration/test_actor_runtime.nim`
</verification>

<success_criteria>
- Actor send semantics cover primitives, frozen graphs/closures, mutable graphs, and capability rejection.
- Mailbox-full behavior parks actor senders instead of blocking worker threads.
- The Phase 1.5 frozen-closure contract is consumed directly through `deep_frozen/shared`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-actor-runtime/02-03-SUMMARY.md`
</output>
