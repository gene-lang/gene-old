---
phase: 02-actor-runtime
plan: 05
type: execute
wave: 5
depends_on:
  - 02-04
files_modified:
  - docs/handbook/actors.md
  - docs/handbook/freeze.md
  - docs/thread_support.md
  - testsuite/10-async/actors/1_send_expect_reply.gene
  - testsuite/10-async/actors/2_frozen_vs_mutable_send.gene
  - testsuite/10-async/actors/3_stop_semantics.gene
autonomous: true
requirements:
  - ACT-02
must_haves:
  truths:
    - "Users can find the Phase 2 actor API and understand how it differs from the legacy thread surface."
    - "Black-box actor examples prove scheduler/send/reply/stop behavior through real Gene programs."
    - "Legacy thread docs remain explicit compatibility boundaries rather than being removed in Phase 2."
  artifacts:
    - path: docs/handbook/actors.md
      provides: "User-facing actor runtime handbook page"
      contains: "gene/actor"
    - path: testsuite/10-async/actors/1_send_expect_reply.gene
      provides: "Gene-level actor reply example"
      contains: "# Expected:"
    - path: testsuite/10-async/actors/3_stop_semantics.gene
      provides: "Gene-level actor stop/drop behavior example"
      contains: "# Expected:"
  key_links:
    - from: docs/handbook/actors.md
      to: docs/thread_support.md
      via: "compatibility boundary narrative"
      pattern: "spawn|Thread"
    - from: testsuite/10-async/actors/*.gene
      to: src/gene/stdlib/actor.nim
      via: "real Gene actor API execution"
      pattern: "gene/actor"
---

<objective>
Close Phase 2 by publishing the actor API contract, preserving the thread compatibility boundary, and adding black-box actor regression programs that exercise the finished runtime.

Purpose: make the shipped Phase 2 behavior discoverable and verify it at the Gene-language level instead of only through Nim integration tests.
Output: actor handbook docs, compatibility-boundary updates, and actor `.gene` regression programs.
</objective>

<execution_context>
@/Users/gcao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gcao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-actor-runtime/02-RESEARCH.md
@.planning/phases/02-actor-runtime/02-PATTERNS.md
@.planning/phases/02-actor-runtime/02-VALIDATION.md
@docs/handbook/freeze.md
@docs/thread_support.md
@testsuite/10-async/threads/1_send_expect_reply.gene
@testsuite/10-async/threads/2_keep_alive_reply.gene
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add black-box Gene actor semantics programs</name>
  <files>testsuite/10-async/actors/1_send_expect_reply.gene, testsuite/10-async/actors/2_frozen_vs_mutable_send.gene, testsuite/10-async/actors/3_stop_semantics.gene</files>
  <read_first>
    - .planning/phases/02-actor-runtime/02-VALIDATION.md
    - testsuite/10-async/threads/1_send_expect_reply.gene
    - testsuite/10-async/threads/2_keep_alive_reply.gene
  </read_first>
  <action>Create a new `testsuite/10-async/actors/` lane with short `# Expected:` programs covering reply futures, frozen-vs-mutable send behavior, and stop/drop semantics. Keep them black-box and self-contained so they exercise the real `gene/actor/*` API instead of Nim-only harness helpers.</action>
  <verify>
    <automated>./testsuite/run_tests.sh testsuite/10-async/actors/1_send_expect_reply.gene && ./testsuite/run_tests.sh testsuite/10-async/actors/2_frozen_vs_mutable_send.gene && ./testsuite/run_tests.sh testsuite/10-async/actors/3_stop_semantics.gene</automated>
  </verify>
  <acceptance_criteria>
    - The testsuite folder demonstrates reply, send-tier, and stop behavior through user-visible Gene code.
    - Each file has deterministic `# Expected:` output and no dependency on thread-only APIs.
  </acceptance_criteria>
  <done>Actor behavior is covered by black-box language tests in addition to the Nim integration/unit suites.</done>
</task>

<task type="auto">
  <name>Task 2: Document the actor API and Phase 2 compatibility boundary</name>
  <files>docs/handbook/actors.md, docs/handbook/freeze.md, docs/thread_support.md</files>
  <read_first>
    - docs/handbook/freeze.md
    - docs/thread_support.md
    - .planning/phases/01.5-freezable-closures/01.5-02-SUMMARY.md
    - .planning/phases/02-actor-runtime/02-RESEARCH.md
  </read_first>
  <action>Add a focused actor handbook page that documents `gene/actor/enable`, `gene/actor/spawn`, `.send`, `.send_expect_reply`, `ctx.reply`, and stop behavior. Update `docs/handbook/freeze.md` to show the Phase 2 frozen-send fast path and keep `docs/thread_support.md` explicit about the surviving compatibility boundary: thread-first surfaces remain available in Phase 2 but are no longer the primary public concurrency API.</action>
  <verify>
    <automated>nim c -r tests/integration/test_thread.nim && ./testsuite/run_tests.sh testsuite/10-async/actors/1_send_expect_reply.gene testsuite/10-async/actors/2_frozen_vs_mutable_send.gene testsuite/10-async/actors/3_stop_semantics.gene testsuite/10-async/threads/1_send_expect_reply.gene testsuite/10-async/threads/2_keep_alive_reply.gene</automated>
  </verify>
  <acceptance_criteria>
    - Actor docs explain the new primary API without erasing the Phase 4 thread-removal boundary.
    - Freeze and thread docs stay consistent with the locked Phase 2 decisions.
  </acceptance_criteria>
  <done>Phase 2 is documented from both sides: new actor usage and unchanged legacy thread compatibility boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation/examples → users | Misstated API/compatibility guidance would cause incorrect actor or thread usage here |
| testsuite programs → runtime | Black-box actor programs must execute the real public API rather than internal hooks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Repudiation | `docs/handbook/actors.md` | mitigate | Document the shipped Phase 2 API and locked mailbox/exception semantics explicitly so behavior matches code and tests |
| T-02-14 | Denial of Service | testsuite actor programs | accept | The new tests are small targeted programs with bounded runtime; CI cost is low and controlled |
| T-02-15 | Tampering | compatibility docs | mitigate | Keep thread-support docs explicit that legacy thread surfaces remain available through Phase 2 and are not removed here |
</threat_model>

<verification>
Run the new actor tests plus legacy thread examples:

- `./testsuite/run_tests.sh testsuite/10-async/actors/1_send_expect_reply.gene testsuite/10-async/actors/2_frozen_vs_mutable_send.gene testsuite/10-async/actors/3_stop_semantics.gene`
- `./testsuite/run_tests.sh testsuite/10-async/threads/1_send_expect_reply.gene testsuite/10-async/threads/2_keep_alive_reply.gene`
- `nim c -r tests/integration/test_thread.nim`
</verification>

<success_criteria>
- The actor API is documented under the handbook with Phase 2 semantics.
- Thread docs remain as an explicit compatibility boundary rather than being removed.
- Gene-level actor regression programs cover reply, send tiers, and stop semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/02-actor-runtime/02-05-SUMMARY.md`
</output>
